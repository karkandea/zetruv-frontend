# Optional override for backend API base URL.
# Production builds default to same-origin /api/v1 when this is left blank.
# Leave blank during local frontend-only development to use mock data.
VITE_API_BASE_URL=
