# Full backend API prefix, including /api/v1.
# Leave blank only for local frontend-only development that intentionally uses mock data.
VITE_API_BASE_URL=

# Optional storefront URL used by the admin console for the "Open storefront" link.
VITE_STOREFRONT_URL=
