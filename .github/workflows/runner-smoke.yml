name: Runner Smoke

on:
  push:
    branches:
      - dev

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Runner alive
        run: echo "GitHub-hosted runner is working"
